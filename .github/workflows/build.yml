name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions using ${{ github.ref }}
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🤖 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository}}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "💻 The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "ℹ️ This job's status is ${{ job.status }}."

  Build-an-UnitTest:
    runs-on: ubuntu-latest
    needs: ["Explore-GitHub-Actions"]
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with:
          distribution: 'adopt' # Installiert die AdoptOpenJDK-Version von Java
          java-version: '11'
      - name: Build with Maven
        run: mvn clean install
      - name: Run tests with Maven
        run: mvn test
      - name: debuggin tests
        run: mvn -Dtest=CalculatorTest test -B -X

  Simulating-Deploy:
    runs-on: ubuntu-latest
    needs: ["Build-and-UnitTest"]
    concurrency:
      group: deploy_and_integration
    steps:
      - run: echo "⏱️simulating deploy on Cluster"
      - name: simulating Time
        uses: GuillaumeFalourd/wait-sleep-action@v1
        with:
          time: '2m'

  Simulating-Integration:
    runs-on: ubuntu-latest
    needs: ["Simulating-Deploy"]
    concurrency:
      group: deploy_and_integration
    steps:
      - run: echo "⏱️simulating integrationtests"
      - name: simulating Time
        uses: GuillaumeFalourd/wait-sleep-action@v1
        with:
          time: '5m'
